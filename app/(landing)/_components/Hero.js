import React from "react";

const Hero = () => {
  return (
    <div className="h-dvh">
      {/* Background will be handled by the animated space background */}
    </div>
  );
};

export default Hero;
